# ---- Zungza Shop ----
Options -Indexes
DirectoryIndex index.php

<IfModule mod_rewrite.c>
  RewriteEngine On
  # Nếu web nằm trong thư mục con (VD /shop) thì bỏ dấu # dòng dưới và sửa đường dẫn
  # RewriteBase /shop/

  # Chuyển về https (bỏ # nếu hosting đã có SSL)
  # RewriteCond %{HTTPS} off
  # RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

  RewriteRule ^(app|views|data|admin/modules)(/|$) - [F,L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ index.php?_path=$1 [QSA,L]
</IfModule>

# Chặn tải file cấu hình, SQL, tài liệu
<FilesMatch "^(config\.php|config\.sample\.php|database\.sql|README\.md|.*\.lock|.*\.sqlite.*)$">
  Require all denied
</FilesMatch>

<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpeg "access plus 30 days"
  ExpiresByType image/png "access plus 30 days"
  ExpiresByType image/webp "access plus 30 days"
  ExpiresByType image/svg+xml "access plus 30 days"
  ExpiresByType text/css "access plus 7 days"
  ExpiresByType application/javascript "access plus 7 days"
</IfModule>
